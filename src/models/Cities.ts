export interface Cities{
    id ?:number;
    name ?:String;
    state_id ?:number;
    Star ?:number;
    stateStatus ?:number;
    image_url?: string;
    overview?: string;
    description?: string;
}