export interface States{
    id ?:number;
    name ?:String;
    country_id ?:number;
    Star ?:number;
    stateStatus ?:number;
    image_url?: string;
    overview?: string;
    description?: string;
}