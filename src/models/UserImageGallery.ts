export interface UserImageGallery{
    id ?: number;   
    imageTitle ?: string;   
    imageIcon ?: String;   
    createdDate ?: Date;    
    total_like ?: number;    
    total_comment ?: number;    
    total_share ?: number;    
    userId ?: number;    
    gallery_Id ?: number;    
    name ?: String;    
    isBookMarked ?: number;    
}